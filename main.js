let isTravel = true;
let isPlaying = true;
const SlimeEnemy = new Slime(80, 80, ctx, 500, 100, "rgba(148,65,157,0.78)")

isTravel = false;

CreateField()
Travel()

//startBattle(SlimeEnemy);
